export function add(x, y){
    return x + y;
}
export function sub(x, y){
    return x - y;
}

export const mult = (x, y) => x * y;

export function div(x, y){
    return y ? x/y : 'divide by zero error';
}